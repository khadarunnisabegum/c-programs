#include <stdio.h>
#include <string.h>
int main()
{
    char s[50];
    int i;
    printf("enter the string");
    gets(s);
    for(i=0;s[i]!='\0';i++)
    {
        if((i%2)==0)
        s[i]=tolower(s[i]);
        else
        s[i]=toupper(s[i]);
    }
    printf("\n%s",s);

    return 0;
}
