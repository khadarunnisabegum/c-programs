#include<stdio.h>
int fact(int);
void main()
{
    int factorial,n;
    printf("enter a number:");
    scanf("%d",&n);
    factorial=fact(n);
    printf("factorial value of given number is:%d",factorial);
}
int fact(int n)
{
  int i,f=1;
  for(i=1;i<=n;i++)
  {
      f=f*i;
  }
    return f;
}
