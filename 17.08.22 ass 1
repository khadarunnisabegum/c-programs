#include<stdio.h>
int main()
{
int i,n,numbers[50];
int sum=0,total;
scanf("%d",&n);
for(i=0;i<n;i++)
{
    scanf("%d",&numbers[i]);
    sum=sum+numbers[i];
}
    n++;
    total=(n*(n+1))/2;
    printf("missing number is %d",total-sum);
return 0;
}
